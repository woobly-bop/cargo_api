# cargo-control-api — pinned dependency manifest
# NOTE: These versions are intentionally OLD and vulnerable.
# This is the file PRAS performs Software Composition Analysis (SCA) against.
# You do NOT need to `pip install` these to run a PRAS scan — PRAS reads the
# manifest statically. If you do install, use a throwaway virtualenv.

# ---- Web framework stack ----
Flask==0.12.2
Werkzeug==0.14.1
Jinja2==2.10
itsdangerous==0.24
MarkupSafe==1.0
click==6.7

# ---- HTTP / networking ----
requests==2.19.1
urllib3==1.23
chardet==3.0.4
certifi==2018.4.16
idna==2.7

# ---- Config / serialization ----
PyYAML==5.1

# ---- Crypto / auth ----
cryptography==2.3
pyOpenSSL==18.0.0
PyJWT==1.5.3

# ---- Data / persistence ----
SQLAlchemy==1.3.0
numpy==1.16.0

# ---- Media / parsing ----
Pillow==6.0.0
lxml==4.2.5

# ---- Remote ops ----
paramiko==2.4.1

# ---- Serving ----
gunicorn==19.7.1
